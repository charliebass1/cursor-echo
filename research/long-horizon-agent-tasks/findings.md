# Findings

Read `DELIVERABLE.md` first — it leads with the answer to the research question. This file is a short index of the final theme clusters; detailed Stage 2 synthesis lives in `output-b-themes-reliability.md`.

## Clustered Themes

1. Cloud startup and environment reliability.
2. Context and instruction retention.
3. Parallel agent and worktree orchestration.
4. Review, verification, and handoff friction.
5. Task sizing, specification, and delegation discipline.
6. Cost, permissions, and security governance.

## Strongest Current Candidates

The Stage 2 2x2 places these themes in the `Now` tier:

- Cloud startup and environment reliability.
- Parallel agent and worktree orchestration.
- Review, verification, and handoff friction.

The `Investigate` tier contains:

- Context and instruction retention.
- Task sizing, specification, and delegation discipline.

The `Watch` tier contains:

- Cost, permissions, and security governance.

## Reliability Note

The internal recode check sampled 6 of the 30 originally retained non-grounding rows. Exact primary-code agreement was 5 of 6, or 83%. This is an internal consistency check, not inter-rater reliability.

## Verification Note (2026-06-23)

A wrap-up pass re-checked the load-bearing sources. Four `cursor/cursor` issue rows were unverifiable (404) and were dropped, leaving 26 retained rows. Only theme T2 lost evidence: its severity drops from S1–S2 to S1 and its independent source families from 3 to 2, so it remains an Investigate item rather than a Now item. T1, T3, and T4 (the Now tier) were unaffected, and T3/T4 gained corroborating forum threads. See `output-a-coverage-source-log.md` for the full verification log.
