{
  "name": "cursor-session-coach",
  "version": "0.1.0",
  "description": "Analyzes exported Cursor chat transcripts, tags friction patterns, and generates prompt-rewrite + automation (.cursor/rules, .cursor/hooks.json, .cursor/skills) suggestions using Cursor's own SDK.",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "tsc --noEmit",
    "analyze": "tsx src/index.ts",
    "analyze:mock": "tsx src/index.ts --mock"
  },
  "dependencies": {
    "@cursor/sdk": "^1.0.10"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.6.0",
    "tsx": "^4.19.0"
  },
  "engines": {
    "node": ">=22"
  }
}
